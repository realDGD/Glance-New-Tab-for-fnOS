# Glance New Tab for fnOS 隐私说明

## 扩展保存什么

扩展使用 Chrome 的同步存储保存以下非敏感设置：

- 新标签页要打开的网址；
- fnOS 根网址与登录状态检测网址；
- 是否启用登录恢复、会话保活及其时间间隔；
- 扩展界面的自动、浅色或深色外观选择。

自动识别或手动填写的局域网 Glance 地址使用 Chrome 本地存储，仅保存在当前设备，不会通过 Chrome 同步到其他设备。它可能包含 NAS 的私有 IP 和 Docker 映射端口。

一次恢复流程的目标网址、当前阶段、重试次数，以及“本次浏览器会话已经完成首次预热”的标记会暂存在 Chrome 的会话存储中；浏览器会话结束后这些数据会被清除。

## 扩展不保存什么

扩展不会读取、复制、导出或保存：

- fnOS 用户名和密码；
- fnOS token；
- 浏览器 Cookie；
- Glance 配置和 GitHub Token；
- 浏览历史。

扩展没有申请 Chrome 的 `cookies`、`history`、`webRequest` 或 `webRequestBlocking` 权限。

## 网络请求

扩展会向用户设置的 fnOS `5ddd.com` 或 `fnos.net` 地址发起会话检测请求。用户明确允许局域网识别后，扩展也会访问该 NAS 上的健康检查地址。

Docker 自动识别不会扫描端口。它只在两分钟的识别窗口内检查用户随后从 fnOS 桌面打开的、位于同一 NAS 主机上的网页，并同时验证 Glance 的 `/api/healthz` 和页面结构。请求不会发送到扩展作者或其他第三方服务。

局域网主机权限是可选权限。Chrome 只会在用户点击允许或保存手动填写的局域网地址时请求，并按具体 NAS 主机授权；较新版本的 Chrome 还可能显示浏览器级“访问本地网络”确认。扩展没有默认获得所有局域网页面的访问权。

## 数据共享

扩展不包含统计、广告或遥测，不会向开发者传输数据。
