# Glance New Tab for fnOS 隐私说明

## 扩展保存什么

扩展使用 Chrome 的同步存储保存以下非敏感设置：

- 新标签页要打开的网址；
- fnOS 根网址与登录状态检测网址；
- 是否启用登录恢复、会话保活及其时间间隔；
- 扩展界面的自动、浅色或深色外观选择。

一次恢复流程的目标网址、当前阶段、重试次数，以及“本次浏览器会话已经完成首次预热”的标记会暂存在 Chrome 的会话存储中；浏览器会话结束后这些数据会被清除。

## 扩展不保存什么

扩展不会读取、复制、导出或保存：

- fnOS 用户名和密码；
- fnOS token；
- 浏览器 Cookie；
- Glance 配置和 GitHub Token；
- 浏览历史。

扩展没有申请 Chrome 的 `cookies`、`history`、`webRequest` 或 `webRequestBlocking` 权限。

## 网络请求

扩展只会向用户设置的 fnOS `5ddd.com` 或 `fnos.net` 地址发起会话检测请求。检测端点必须只返回 `ok` 才会被视为登录有效。请求不会发送到扩展作者或其他第三方服务。

## 数据共享

扩展不包含统计、广告或遥测，不会向开发者传输数据。
